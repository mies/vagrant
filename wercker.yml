box: wercker/ruby
build:
    steps:
        # - script:
        #     name: apt-get install bsdtar
        #     code: sudo apt-get install -y bsdtar
        - bundle-install
        - script:
            name: rake test
            code: |
                sudo chmod go-w /tmp
                sudo bundle exec rake test:unit --trace
